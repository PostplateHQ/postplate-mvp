/**
 * Default store key for local seed data in backend/db/data.json.
 * Opaque format: 2-digit location slot + 4-digit random (e.g. 01-7402).
 */
const DEFAULT_SEED_STORE_ID = "01-7402";

module.exports = { DEFAULT_SEED_STORE_ID };
